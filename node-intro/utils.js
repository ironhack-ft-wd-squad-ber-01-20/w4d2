const divide = (a, b) => {
  return a / b;
};

const remainder = (a, b) => {
  return a % b;
};

module.exports = {
  // divide: divide,
  // remainder: remainder,
  divide,
  remainder,
  foo: "BAR"
};
